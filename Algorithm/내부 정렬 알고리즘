정렬 방식 

- 비교 기반 알고리즘
선택 정렬(selection sort) (O(n^2))
버블 정렬(bubble sort) (O(n^2))
삽입 정렬(insertion sort) (O(n^2))
셸 정렬(shell sort) (O(n log n))

퀵 정렬(quick sort) (O(n log n))
합병 정렬(merge sort) (O(n log n))
힙 정렬(heap sort) (O(n log n))

- 데이터 분포 기반 알고리즘
계수 정렬(counting sort) (O(n))
기수 정렬(radix sort) (O(n))
버킷 정렬(bucket sort) (O(n))

안정적 stable 정렬
- 안정적 정렬: 동일한 키를 가진 요소의 상대적인 순서를 유지하는 정렬
- 불안정한 정렬: 동일한 키를 가진 요소의 상대적인 순서를 유지하지 않는 정렬
- 안정적 정렬 예시: 합병 정렬, 삽입 정렬, 계수 정렬
- 불안정한 정렬 예시: 퀵 정렬, 선택 정렬, 버블 정렬

제자리 in-place 정렬
- 제자리 정렬: 추가적인 메모리 공간을 거의 사용하지 않고 정렬
- 비제자리 정렬: 추가적인 메모리 공간을 사용하는 정렬
- 제자리 정렬 예시: 퀵 정렬, 버블 정렬, 선택 정렬, 삽입 정렬
- 비제자리 정렬 예시: 합병 정렬, 계수 정렬, 기수 정렬, 버킷 정렬